apiVersion: 1

datasources:
  - name: InfluxDB_v2.7_Flux
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: homeiot
      defaultBucket: sensors
      tlsSkipVerify: true
    secureJsonData:
      token: $__env{INFLUXDB_TOKEN}